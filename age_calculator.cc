/**age calculator.
 * result in year
 * result in month
 * result in day
 * result in hour
 * result in minute
 * result in second
 */
#include <iostream>
#include <stdio.h>
using namespace std;
class ageCalculator
{
    char dob[9]; // to store DOB from user
    short dobDay;
    short dobMonth;
    short dobYear;

public:
    void calculateMyAge();
    {
        // atoi() :
    }
};